# 上传到 GitHub Pages —— 操作步骤

目标网址：**https://caizhufu329-star.github.io/RUI/**

> 说明：你的 GitHub 集成没有建库权限（MCP 返回 403），且本机没有 GitHub token（只有 gitee 的），
> 所以最后一步上传需要你在 GitHub 网页上操作。整个过程约 2 分钟，全程拖拽，不用命令行。

---

## 第 1 步：上传文件

1. 打开 **https://github.com/caizhufu329-star/RUI**
2. 点击右上角 **Add file** → **Upload files**
3. 打开本地文件夹 **`D:\黔货出海\publish-single`**，把里面的内容**全部拖进上传区**：
   - `index.html`（160 KB，Demo 主系统，CSS/JS 已全部内联，**单文件即可运行**）
   - `.nojekyll`（空文件，让 Pages 原样服务，防止 Jekyll 干扰）
   - `station/` 整个文件夹（视频工作站，可选）
4. 拉到底部，Commit message 填 `添加黔货出海Demo系统`，点 **Commit changes**

> 拖拽文件夹时 GitHub 会保留目录结构，`station/assets/...` 会自动建好。

---

## 第 2 步：开启 GitHub Pages

1. 在仓库页点 **Settings**（右上角齿轮）
2. 左侧菜单点 **Pages**
3. **Source** 选 **Deploy from a branch**
4. **Branch** 选 `main`，文件夹选 `/ (root)`，点 **Save**
5. 等约 1—2 分钟，页面顶部会出现网址：
   **https://caizhufu329-star.github.io/RUI/**

---

## 第 3 步：加到手机桌面（随时随地点击）

- **iPhone**：Safari 打开网址 → 分享按钮 → 「添加到主屏幕」
- **安卓**：Chrome 打开网址 → 右上角菜单 → 「添加到主屏幕」

之后桌面上会有一个图标，点开就是这个系统。

---

## 文件说明

| 文件 | 大小 | 说明 |
|---|---|---|
| `index.html` | 160 KB | Demo 九大模块，CSS/JS 全内联，**零外链、零依赖** |
| `.nojekyll` | 0 KB | 告诉 Pages 不要走 Jekyll 构建 |
| `station/index.html` | 10 KB | 视频工作站（访问 `/station/`） |

访问地址：
- 主系统：https://caizhufu329-star.github.io/RUI/
- 视频工作站：https://caizhufu329-star.github.io/RUI/station/

---

## 已验证

单文件版本本地实测：标题正确、九大视图切换正常、第 09 视图甘特条 16 条、
合规预审 42 项条款渲染成功、**页面错误 0**、**剩余外链 0**。

---

## 注意事项

- **仓库是公开的**（免费版 Pages 的限制）。已确认上传内容只含 Demo 系统，
  **不含计划书 PDF / PPTX**，方案细节与财务测算不会上公网。
- 如果之后想下线，在 Settings → Pages 里点 **Disable** 即可，或把仓库改成 Private
  （改私有后 Pages 会停止工作，需 GitHub Pro 才能私有 Pages）。
- 视频（69 MB）不适合放 GitHub，建议单独传网盘，
  然后把直链发我，我改 `data/plan-data.js` 里 `meta.videoUrl` 一处即可让顶栏「演示视频」按钮生效。
